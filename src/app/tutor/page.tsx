"use client";
import { ArrowRight, BrainCircuit, Calculator, LockKeyhole } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { useApp } from "@/components/app-provider";
export default function Tutor(){const{data}=useApp();const active=data.courses[0];return <AppShell eyebrow="PRIVATE MENTOR MODE" title="No skipped steps. No assumed knowledge."><div className="tutor-layout"><section className="tutor-stage"><div className="tutor-badge"><BrainCircuit/>Rescue me mode</div><p>What are we finding?</p><h2>{active?`${active.code} question patterns`:"Your first calculation path"}</h2><div className="formula-route"><b>Given values</b><i/><b>Missing value</b><i/><b>Final answer</b></div><article><small>YOUR NEXT STEP</small><h3>{active?.materials.length?"Choose a past question to begin":"Add a past-question file first"}</h3><p>{active?.materials.length?"Olodo will identify the target, map every missing value, and explain why each formula comes next.":"The tutor grounds lessons in your course materials instead of inventing a generic syllabus."}</p><button disabled={!active?.materials.length}>Start guided solution <ArrowRight/></button></article></section><aside className="tutor-modes"><h3>Learning modes</h3><div className="selected"><BrainCircuit/><span><b>Rescue me</b>Show every formula and substitution.</span></div><div><Calculator/><span><b>Guide me</b>Let me attempt each calculation.</span></div><div className="locked"><LockKeyhole/><span><b>Test me</b>Unlock after guided practice.</span></div></aside></div></AppShell>}
